<Window x:Class="MStudio.App.Views.GRFFilesInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select BodyKinematics Files"
        Width="550" Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#1E1E2E" CornerRadius="12" BorderBrush="#3A3A5A" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#252535" CornerRadius="12,12,0,0" 
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Height="45">
                    <TextBlock Text="Select BodyKinematics Output Files" FontSize="16" FontWeight="SemiBold" 
                               Foreground="#E0E0E0" VerticalAlignment="Center" Margin="20,0"/>
                    <Button Content="✕" Width="35" Height="35" HorizontalAlignment="Right" 
                            Click="CloseButton_Click" Background="Transparent" Foreground="#888" 
                            BorderThickness="0" FontSize="14" Cursor="Hand" Margin="5,0"/>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="25,20">
                <TextBlock Text="For GRF estimation, please provide the BodyKinematics output file:" 
                           Foreground="#AAA" FontSize="12" Margin="0,0,0,20" TextWrapping="Wrap"/>
                
                <!-- BodyKinematics CSV File -->
                <TextBlock Text="BodyKinematics CSV File (contains COM_x, COM_y, COM_z)" 
                           Foreground="#CCC" FontSize="13" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="BodyKinCsvPath" Height="32" IsReadOnly="True"
                             Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#3A3A5A" 
                             FontSize="12" Padding="8,6" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="1" Content="Browse..." Width="80" Height="32" Margin="10,0,0,0"
                            Click="BrowseBodyKin_Click" Background="#3A3A5A" Foreground="#E0E0E0" 
                            BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Validation Status -->
                <Border x:Name="ValidationPanel" Background="#2A2A3E" CornerRadius="6" Padding="12,10" Visibility="Collapsed">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="ValidationIcon" Text="✓" Foreground="#7ED957" FontSize="14" FontWeight="Bold"/>
                            <TextBlock x:Name="ValidationText" Text="File validated successfully" Foreground="#7ED957" FontSize="12" Margin="8,0,0,0"/>
                        </StackPanel>
                        <TextBlock x:Name="ValidationDetails" Text="" Foreground="#888" FontSize="11" Margin="22,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Error Status -->
                <Border x:Name="ErrorPanel" Background="#4F1E2E" CornerRadius="6" Padding="12,10" Visibility="Collapsed">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✕" Foreground="#E06C75" FontSize="14" FontWeight="Bold"/>
                            <TextBlock x:Name="ErrorText" Text="Invalid file" Foreground="#E06C75" FontSize="12" Margin="8,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#252535" CornerRadius="0,0,12,12" Padding="20,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Width="80" Height="32" Click="CloseButton_Click"
                            Background="#3A3A5A" Foreground="#AAA" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="ConfirmButton" Content="Confirm" Width="100" Height="32" Click="ConfirmButton_Click"
                            Background="#4A7DBA" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold"
                            IsEnabled="False" Opacity="0.5">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
